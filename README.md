# Дерево решения

Я реализовал ансамбль деревьев решений с бэггингом и случайной подвыборкой признаков для управления роботом на основе 3 расстояний (слева, спереди, справа). Модель обучается на CSV‑датасете и в интерактивном режиме предсказывает команду: стоп, вперёд, поворот направо или налево.

## 1. Структура проекта

 `tree.h` = Прототипы функций работы с деревом и датасетом.
 `tree.c` = Реализация дерева решений: Gini impurity, построение, предсказание, загрузка данных.
 `beging.h` =  структура `BegModel` и функции бэггинга
 `beging.c` = Реализация бэггинга
 `Smeshariki.c` = Главный модуль
 `robot_data.csv` = Обучающий датасет
 

---

## 2. Дерево решений (Decision Tree)

Дерево решений – это бинарное дерево, где каждый внутренний узел проверяет значение одного признака и сравнивает его с порогом. В зависимости от результата пример направляется в левую или правую ветвь. Лист дерева содержит окончательное предсказание (класс). Дерево строится рекурсивно, на каждом шаге выбирая такое разбиение, которое максимально «очищает» данные (уменьшает примесь Джини).

### Ключевые функции дерева(сами функции в коде,я их поэтому не вставил)

Datas* create_datas(int kol_prim, int kol_priz)	 = Создаёт память для датасета

void del_data(Datas* ds) =	Освобождает всю память, выделенную для датасета

Datas* load_data(char* file) =	Загружает датасет из CSV-файла
Функция	Назначение
static float gini(int *prav, int num_prav)	Вычисляет примесь Джини для массива меток. Используется для оценки качества разбиения.
static int popul_class(int *prav, int num_prav)	Находит наиболее часто встречающийся класс в массиве меток (моду). Применяется для определения предсказания в листе дерева.
int max_pred(int* schet_pred_tree, int kol_class)	Принимает массив голосов за каждый класс и возвращает индекс класса с максимальным количеством голосов (argmax).
Построение и обучение дерева
Функция	Назначение
Tree* create_tree(int max_glub, int min_split, int kol_priz)	Создаёт структуру дерева с заданными параметрами: максимальная глубина, минимальное количество образцов для разбиения, количество случайных признаков на узел.
void free_Node(Node* node)	Рекурсивно освобождает память, занятую узлом и всеми его потомками.
void del_tree(Tree* dub)	Освобождает всё дерево: вызывает free_Node для корня и освобождает структуру дерева.
static void best_split(...)	По заданному подмножеству признаков ищет лучший признак и порог разбиения, максимизирующие прирост (gain) по Gini. Результат возвращает через указатели.
static Node* build_tree(...)	Рекурсивно строит дерево: проверяет условия остановки, формирует случайное подмножество признаков, вызывает best_split, создаёт внутренние узлы или листья.
void fit_tree(Tree* dub, Datas* ds)	Обучает дерево на полном датасете: создаёт массив индексов всех примеров и вызывает build_tree, сохраняя корень в dub->koren.
Предсказание
Функция	Назначение
static int predict_node(Node* node, float *prim)	Рекурсивно обходит дерево, начиная с указанного узла, и возвращает предсказанный класс для одного образца.
int predict_tree(Tree* dub, float* sample)	Публичная функция предсказания: вызывает predict_node от корня дерева.
Утилиты
Функция	Назначение
void random_priz(int *index, int n)	Перемешивает массив целых чисел (алгоритм Фишера-Йетса). Используется для случайной подвыборки признаков.
## 3. Бэггинг (Bagging)

1. Bootstrap aggregating
Обучается **множество разных деревьев**. Каждое дерево получает свою обучающую выборку, сгенерированную рандомно из исходного датасета. Размер выборки совпадает с размером исходной.

2. Обучение ансамбля
- Для каждого дерева создаётся bootstrap‑выборка (`bootstrap_sample`).
- Дерево обучается на этой выборке (`fit_tree`).
- Все деревья сохраняются в массиве `trees` структуры `BegModel`.

3. Предсказание
При предсказании опрашиваются все деревья. Каждое дерево говорит за класс, который оно предсказало. Итоговым ответом становится класс, набравший **максимальное количество голосов** (функция `max_pred`).
