# Дерево решения

Я реализовал ансамбль деревьев решений с бэггингом и случайной подвыборкой признаков для управления роботом на основе 3 расстояний (слева, спереди, справа). Модель обучается на CSV‑датасете и в интерактивном режиме предсказывает команду: стоп, вперёд, поворот направо или налево.

## 1. Структура проекта

 `tree.h` = Прототипы функций работы с деревом и датасетом.
 
 `tree.c` = Реализация дерева решений: Gini impurity, построение, предсказание, загрузка данных.
 
 `beging.h` =  структура `BegModel` и функции бэггинга
 
 `beging.c` = Реализация бэггинга
 
 `Smeshariki.c` = Главный модуль
 
 `robot_data.csv` = Обучающий датасет
 

---

## 2. Дерево решений (Decision Tree)

Дерево решений – это бинарное дерево, где каждый внутренний узел проверяет значение одного признака и сравнивает его с порогом. В зависимости от результата пример направляется в левую или правую ветвь. Лист дерева содержит окончательное предсказание (класс). Дерево строится рекурсивно, на каждом шаге выбирая такое разбиение, которое максимально «очищает» данные (уменьшает примесь Джини).

### Ключевые функции дерева(сами функции в коде,я их поэтому не вставил)
```c
Datas* create_datas(int kol_prim, int kol_priz)	== Создаёт память для датасета

void del_data(Datas* ds) ==	Освобождает всю память, выделенную для датасета

Datas* load_data(char* file) ==	Загружает датасет из CSV-файла

static float gini(int *prav, int num_prav) ==	Вычисляет примесь Джини для массива меток. Используется для оценки качества разбиения.

static int popul_class(int *prav, int num_prav)==	Находит наиболее часто встречающийся класс в массиве для определения предсказания в листе дерева.

int max_pred(int* schet_pred_tree, int kol_class)== Возвращает индекс самого частого предсказания

Tree* create_tree(int max_glub, int min_split, int kol_priz) ==	Создаёт структуру дерева 

void free_Node(Node* node)==	освобождает память узла

void del_tree(Tree* dub)==	Освобождает всё дерево

static void best_split(Datas *ds,int *index,int kol_index)== Ищет лучший признак и порог разбиения, максимизирующие прирост Gini

static Node* build_tree(Datas* ds,int* index,int kol_ind,int depth,int max_depth,int min_split,int kol_priz_sub,int max_pri)==	Рекурсивн строит дерево: проверяет условия остановки,формирует рандомное подмножество признаков, вызывает best_split, создаёт внутренние узлы или листья.

void fit_tree(Tree* dub, Datas* ds)==	Обучает дерево

static int predict_node(Node* node, float *prim)==	Рекурсивно обходит дерево, начиная с указанного узла, и возвращает предсказанный класс для одного образца.

int predict_tree(Tree* dub, float* sample)==Вызов predict->node с корня

void random_priz(int *index, int n)==	Перемешивает массив призноков для рандомизации
```
---

## 3. Бэггинг (Bagging)

Беггинг- обучает много деревьев на разных случайных подвыборках исходных данных и усредняет их предсказания ,что понижает переобучение и повышает точность

# Bootstrap aggregating
Обучается множество разных деревьев. Каждое дерево получает свою обучающую выборку, сгенерированную рандомно. Размер выборки совпадает с размером исходной.

# Обучение ансамбля
- Для каждого дерева создаётся bootstrap‑выборка (`bootstrap_sample`).
- Дерево обучается на этой выборке (`fit_tree`).
- Все деревья сохраняются в массиве `trees` структуры `BegModel`.

# Предсказание
При предсказании опрашиваются все деревья. Каждое дерево говорит за класс, который оно предсказало. Итоговым ответом становится класс, набравший **максимальное количество голосов** (функция `max_pred`).

# Функции
```c
bootstrap_sample(Datas* ds)==Возвращает рандомные примеры для обучения

 create_beg_model(int kol_tree, int max_glub,int min_split,int kol_priz_sub)== Создание модели
 
 freeModel(BegModel* mod)== Освобождение памяти 
 
 fit_beg(BegModel* mod,Datas* ds)== Обучает каждое дерево с bootstrap
 
 pred_beg(BegModel *mod,float* sample)== Сохраняет предсказание каждого дерева и выводит индекс самого частого 

```

## Рандомизация признаков 
Случайная подвыборка признаков - на каждом узле выбирает рандомные признаки и их рандомное количество.
Это увеличивает разнообразие деревьев, снижает переобучение и повышает точность предсказания 

# Функции
```c
void random_priz(int *index,int n)== рандомное распределение признаков 

```
Где именно происходит рандомизация:сначала делаем признаки рандомными,а затем записываем столько,сколько мы поставили признаков,и после этого делаем лучшие разбиение
<img width="1074" height="502" alt="image" src="https://github.com/user-attachments/assets/be65caf6-2a7c-458e-8231-686e42ff7d44" />

---

## Что выводит?

Всё это вызывается и потом всё выводиться в файле **Smeshariki.c**

Когда мы запустили программу ,то после этого мы можем вводить три наших параметра расстояния:лево,вперёд,вправо
<img width="743" height="62" alt="image" src="https://github.com/user-attachments/assets/6de9e9ec-c0c1-4e8f-ad8c-b8264e1e3090" />

После этого мы можем увидеть 5 ответов:Остановка,прямо,налево,направо,ошибка
<img width="759" height="40" alt="image" src="https://github.com/user-attachments/assets/a7861fc2-735b-49e9-9b4f-ee08e5782cb3" />


При запуске программы мы вводим :имя датасета,количество деревьев,максимальную глубину,минимальное разбиение и количество признаков
<img width="732" height="51" alt="image" src="https://github.com/user-attachments/assets/ff232446-82c8-46e2-bc53-182d4e931b9a" />

**Количество деревьев**(kol_tree)	- от 1 до бесконечности	(Чем больше ,тем точнее, но дольше.)

**Максимальная глубина**(max_glub)	- от 1 до бесконечности (Ограничивает переобучение .)

**Минимальное разбиение**(min_split) - от 	 до N	(Делает так,чтобы не было  мелкиех листьев.)

**Случайных признаков на узел**(kol_priz_sub) - от 	1 до kol_priz (Чем меньше признаков,тем разнообразнее дерево, но таким образом можно случайно потерять важные признаки.)

### Как формируется датасет 
Значения признаков (лево,вперёд,право) подобраны так, чтобы всё было логично логику:

Класс 0(стоп): вперёд <= 0.2 (препятствие спереди), лево и право могут быть любыми.

Класс 1(вперёд): вперёд >= 0.6, лево и право не слишком маленькие (≥0.3).

Класс 2( направо):лево > право (справа меньше места), вперёд среднее (0.3…0.6).

Класс 3(налево): лево < право (слева меньше места), вперёд среднее.

<img width="321" height="467" alt="image" src="https://github.com/user-attachments/assets/9ef1c08c-6a74-4182-b31e-83d7884f1f8b" />

---

## Компиляция

 Ручной 

Компилируем файлы Smeshariki.c. ,beging.c и tree.c в один исполняющий файл ,например robot
<img width="375" height="47" alt="image" src="https://github.com/user-attachments/assets/b5bb8718-68a6-4d0a-b236-c259b23c5653" />

Затем запускаем ,вводя нужные параметры
<img width="288" height="47" alt="image" src="https://github.com/user-attachments/assets/16786159-522b-4a8d-9f22-0bf5e52e36b1" />

# Конец 
